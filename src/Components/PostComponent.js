import React from "react";

const PostComponent = ({ post }) => {
	const {
		postId,
		username,
		picUrl,
		post,
		postDescription,
		upvotes,
		downvotes,
		tags,
		isBookmarked,
	} = post;
	return (
		<div>
			<div></div>
		</div>
	);
};

export default PostComponent;
